- Investigated TuSecre production WhatsApp onboarding and receipt-processing issues on Docker deployment at /opt/tusecre.
- Confirmed active app container uses DATABASE_URL=file:./data/dev.db with bind mount /opt/tusecre/data -> /app/data from docker-compose.yml.
- Verified Prisma migrations/client regeneration status; production DB schema updated manually/baselined enough for Receipt fields: type, retentionAmount, retentionPercentage, aeatSummary, aeatSources, validationIssues, reviewRequired.
- Discovered WhatsApp sender number for testing is 34640291370 (phone 640291370 without country code), not the Telegram sender id.
- Confirmed User row now exists in active DB for phone 34640291370 with createdAt 2026-04-18T15:38:41.937Z.
- Confirmed app logs show webhook receives messages/images from 34640291370 and responds from this container.
- Receipt extraction failure root cause identified from logs: Gemini model gemini-1.5-flash-8b returns 404 for generateContent; extractor fallback ended on that unavailable model. Recommended removing it from ANALYSIS_MODELS.
- Remaining open issue at flush time: after rebuild, image messages still trigger welcome sequence again, suggesting deployed /app/dist/routes/whatsapp.js may not include the latest onboarding guard fix or container is running stale compiled code. Next recommended checks: inspect /app/dist/routes/whatsapp.js for shouldSendWelcome/existingActivity logic, or perform docker compose down && docker compose build --no-cache && docker compose up -d.
- Continued debugging TuSecre Docker persistence and webhook flow.
- Confirmed onboarding logic now behaves correctly within a single running app instance: first message from 34640291370 yields isNewUser:true, second message without restart yields isNewUser:false.
- Confirmed repeated post-deploy welcome was not due to webhook branching bug alone; user existence before first post-build message was the key variable.
- Confirmed text chat model list in src/services/gemini.ts was updated from obsolete Gemini models to newer ones; earlier failures were 404 model unavailable errors.
- Confirmed image receipt flow reaches WhatsApp media download and user lookup correctly once onboarding is bypassed.
- Receipt extraction still failed in runtime because src/services/agents/receipt-extractor.ts was using obsolete models (gemini-2.0-flash / gemini-1.5-flash); recommendation was to update extractor model list similarly.
- Strong persistence finding: creating/upserting a User inside the container returned success, but host file /opt/tusecre/data/dev.db hash, mtime, and size did not change, proving writes were not landing in the expected bind-mounted DB file.
- Root cause likely identified in .env: there were two DATABASE_URL lines, with the later old relative path DATABASE_URL="file:./data/dev.db" overriding the intended absolute path DATABASE_URL=file:/app/data/dev.db.
- Next action recommended to user: remove duplicate DATABASE_URL line from /opt/tusecre/.env so only DATABASE_URL=file:/app/data/dev.db remains, rebuild, confirm with `docker compose exec app sh -lc 'printenv | grep DATABASE_URL'`, then re-run hash/persistence test on /opt/tusecre/data/dev.db.
